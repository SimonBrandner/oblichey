[package]
name = "gday"
version = "0.0.1"
edition = "2021"
build = "src/build.rs"

[build-dependencies]
burn-import = { git = "https://github.com/skewballfox/burn", branch = "legacy_initializer_fix" }

[dependencies]
v4l = "0.14.0"
chrono = "0.4.38"
clap = { version = "4.4.11", features = ["derive"] }
image = { version = "0.24", default-features = false }
burn = { git = "https://github.com/skewballfox/burn", branch = "legacy_initializer_fix" }
burn-ndarray = { git = "https://github.com/skewballfox/burn", branch = "legacy_initializer_fix" }
burn-import = { git = "https://github.com/skewballfox/burn", branch = "legacy_initializer_fix" }
eframe = { version = "0.20", default-features = false, features = [
  "glow",
  "default_fonts",
] }

[lints.rust]
unsafe_code = "forbid"

[lints.clippy]
enum_glob_use = "deny"
unwrap_used = "deny"
pedantic = "deny"
missing_errors_doc = "allow"
must_use_candidate = "allow"
nursery = "deny"
module_name_repetitions = "allow"
